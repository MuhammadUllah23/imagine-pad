<h2>Create a Genre</h2>

<%= render partial: "layouts/error", locals: { object: @genre } %>

<%= form_for(@genre) do |f| %>
    <%= render partial: "form", locals: { f: f}%>

    <%= f.submit %>
<% end %>